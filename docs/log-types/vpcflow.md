# AWS VPC Flow Logs

This document describes the AWS VPC Flow Logs and how they are processed by the Cribl Pack. AWS VPC Flow Logs are a feature that enables you to capture information about IP traffic going in and out of netwoak interfaces in your VPC.

## Log Format

VPC Flow Logs are delivered as plain text files.