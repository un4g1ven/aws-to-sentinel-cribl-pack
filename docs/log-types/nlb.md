# AWS NLB Logs

This document describes the AWS NLB Logs and how they are processed by the Cribl Pack. AWS Network Load Balancer (NLB) Logs provide detailed information about the TCP connections clients make to targets in your NLB.

## Log Format

AWS NLB Logs are delivered as plain text files in an S3 bucket.